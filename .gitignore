# Generated reports — the morning routine rebuilds these; do not commit churn.
reports/
*.sitrep.html

# dashboard.html is the exception: it is the product, and it is committed.
!dashboard.html

# Secrets stay out of the repo and out of the agent's context.
.env
.env.*

# Language debris — extend for whichever toolchain you pick in CLAUDE.md.
__pycache__/
.venv/
node_modules/
.DS_Store
*.egg-info/

# Slice 1: the ledger is a produced artifact for now. ADR-0006 commits it, but
# committing binary churn is premature until the single-writer / commit-on-change
# logic (slice 7) exists. See implementation-notes.md.
state/
